# Production environment
# The frontend is served by the Pulse binary (same origin), so API calls use
# relative URLs (empty value). Do NOT set an absolute host here — that would
# break any deployment not running on localhost:6532.
VITE_API_BASE_URL=
